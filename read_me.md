# Attribute-aware Controllable Menu Editor for Allergen-free Diet Planning

## Explanation

## Implementation

- Our Environment
  - OS: Linux-5.4.0
  - python == 3.9.7
  - conda >= 4.10
    
Run this code to create your virtual environment. (you can skip it, but we recommend creating a new virtual environment for this implementation)
```bash
conda create -n 'env_name' python=3.9.7
```
Run this code to clone the files into your local environment.
```bash
git clone https://github.com/ACMEditor.git
```
Move to the `ACMEditor` folder
```
cd ACMEditor/
```
Run the below code to install the required package for this implementation.
```
#(if you need) pip install --upgrade pip
pip install -r requirements.txt
```

### Code Structure
```
├── data/
├── src/
├── models/
└── README.md
```

### Note

